services:
  - type: web
    name: ryoryocompany-ai-appeal-auth
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server_app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /








